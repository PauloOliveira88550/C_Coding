#include <stdio.h>
#include <string.h>

int verifica(char ch, int *cC, int *cV)
{
	(*cC)=0;
	(*cV)=0;

	if(ch == 'A' || ch == 'a' || ch =='E' || ch == 'e' || ch =='I' || ch == 'i' || ch == 'O' || ch == 'o' || ch == 'U' || ch == 'u')
	{
		(*cV)++;
	}
	else 
	{
		(*cC)++;
	}
	else 
	{
		

	return 0;
}



int main(int argc, int *argv[])
{
	int i=1;
	int *contaCon;
	int *contaVogs;

	for(i<argc;i++)
	{
		verifica((*argv[i]), &contaCon, &contaVogs);
		if((*argv[i])  == ' ')
		{
			printf(" - %d vogais, %d consoantes", contaVogs, contaCon);
			printf("\n");
		}

		printf("%ls", argv[i]);
		
	}

return 0;

}
